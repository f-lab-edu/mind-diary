<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mindDiary.mindDiary.repository.UserRepository">
  <select id = "findByEmail" resultType="com.mindDiary.mindDiary.domain.User">
    SELECT * FROM user WHERE email = #{email}
  </select>

  <select id = "findByNickname" parameterType="String" resultType="com.mindDiary.mindDiary.domain.User">
    SELECT * FROM user WHERE nickname = #{nickname}
  </select>

  <select id = "findById" parameterType="int" resultType="com.mindDiary.mindDiary.domain.User">
    SELECT * FROM user WHERE id = #{id}
  </select>

  <update id="save" useGeneratedKeys="true" keyProperty="id" parameterType="com.mindDiary.mindDiary.domain.User">
    INSERT INTO user(email,nickname,password,role)
    VALUES(#{email}, #{nickname}, #{password}, #{role})
  </update>

  <update id = "updateRole" parameterType="com.mindDiary.mindDiary.domain.User">
    UPDATE user
    SET role = #{role}
    WHERE id = #{id}
  </update>
</mapper>
